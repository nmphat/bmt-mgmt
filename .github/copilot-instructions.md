# Copilot Instructions

This file is wrapper-specific. The older root-level `copilot-instructions.md` documents the broader NDC sanitize workspace; use this file for behavior inside `ndc-wrapper`.

Must always load `~/.copilot/copilot-instructions.md`. Never stop the session, always asks for next actions.
