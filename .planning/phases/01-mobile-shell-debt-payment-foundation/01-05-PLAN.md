---
phase: 01-mobile-shell-debt-payment-foundation
plan: 05
title: Phase 1 no-regression and mobile sweep validation
type: execute
wave: 4
depends_on:
  - 01-02-PLAN.md
  - 01-03-PLAN.md
  - 01-04-PLAN.md
requirements:
  - SHELL-01
  - SHELL-02
  - SHELL-03
  - SHELL-04
  - SHELL-05
  - DEBT-01
  - DEBT-02
  - DEBT-03
  - DEBT-04
  - DEBT-05
  - DEBT-06
  - ADMIN-04
files_modified:
  - .planning/phases/01-mobile-shell-debt-payment-foundation/01-PHASE1-VALIDATION.md
autonomous: false
must_haves:
  truths:
    - "Type-check and build pass after all Phase 1 plans."
    - "Route/access behavior matches the preservation matrix."
    - "360/390/430px sweeps confirm nav, group bar, sheets, and toasts do not block primary actions."
    - "Guest/admin sweeps verify hidden login, public read-only routes, debt QR, group QR, and admin actions."
  artifacts:
    - path: ".planning/phases/01-mobile-shell-debt-payment-foundation/01-PHASE1-VALIDATION.md"
      provides: "Final validation evidence and sweep checklist"
  key_links:
    - from: "01-PHASE1-VALIDATION.md"
      to: "01-PRESERVATION-MATRIX.md"
      via: "route/access checklist comparison"
      pattern: "PRESERVATION-MATRIX"
---

<objective>
Complete the Phase 1 no-regression gate with automated checks and focused human visual verification.

Purpose: Confirm locked decisions, requirements, route access, payment semantics, i18n, and mobile ergonomics survived implementation.
Output: Validation report plus a blocking human verification checkpoint.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-mobile-shell-debt-payment-foundation/01-CONTEXT.md
@.planning/phases/01-mobile-shell-debt-payment-foundation/01-UI-SPEC.md
@.planning/phases/01-mobile-shell-debt-payment-foundation/01-PRESERVATION-MATRIX.md
@src/router/index.ts
@src/App.vue
@src/components/AppHeader.vue
@src/components/BottomNav.vue
@src/views/HomePage.vue
@src/components/HomeDebtTable.vue
@src/components/PaymentQRModal.vue
@src/components/ManualPaymentModal.vue
@src/views/MemberView.vue
@src/views/MemberDetailView.vue
@src/views/DashboardView.vue
@src/locales/messages.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated no-regression checks and write validation report</name>
  <files>.planning/phases/01-mobile-shell-debt-payment-foundation/01-PHASE1-VALIDATION.md</files>
  <read_first>
    - .planning/phases/01-mobile-shell-debt-payment-foundation/01-PRESERVATION-MATRIX.md
    - .planning/phases/01-mobile-shell-debt-payment-foundation/01-CONTEXT.md
    - .planning/phases/01-mobile-shell-debt-payment-foundation/01-UI-SPEC.md
    - src/router/index.ts
    - src/views/MemberView.vue
    - src/views/DashboardView.vue
    - src/components/BottomNav.vue
    - src/components/PaymentQRModal.vue
    - src/components/ManualPaymentModal.vue
    - src/locales/messages.ts
  </read_first>
  <action>
    Run `pnpm type-check` and `pnpm build`. Create `01-PHASE1-VALIDATION.md` with sections for automated commands, route/access results, i18n key coverage, payment contract preservation, accessibility/focus/aria checks, mobile overlap checklist, and remaining observations. Include explicit pass/fail rows for D-01 through D-19 and requirements SHELL-01 through ADMIN-04. Include a concrete route/access table with columns: route, expected guest access, expected admin access, route guard/meta evidence, component gate evidence, observed result, pass/fail. Include a concrete viewport sweep table with columns: width, role, route/surface, bottom nav clear, group bar clear, sheet footer clear, toast clear, primary action reachable, keyboard/focus visible, pass/fail, notes. Verify by reading/grepping source that: BottomNav has exactly `/`, `/members`, `/sessions`; guest login is not visible in header/bottom nav; `/create-session` remains admin-only; MemberView CRUD gates use `isAdmin`; PaymentQRModal has polling cleanup and no auto-close; HomePage payment-complete refresh does not close; modal payload reset occurs only on explicit close; ManualPaymentModal calls `add_manual_payment` only after review confirmation. Also verify accessibility source evidence: BottomNav links have accessible labels/text and focus-visible styling; payment/cash sheets use `role="dialog"` and `aria-modal`; search input has label/aria-label; Details link/button has accessible text; close/Done buttons are 44px tappable and keyboard-focusable.
  </action>
  <verify>
    <automated>pnpm type-check && pnpm build</automated>
  </verify>
  <done>
    Done when `pnpm type-check` and `pnpm build` results are recorded in `01-PHASE1-VALIDATION.md`, the report includes D-01 through D-19 and all Phase 1 requirement IDs, and each route/access/i18n/payment/accessibility/mobile-overlap no-regression guardrail has an explicit pass/fail row plus route/access and viewport sweep tables.
  </done>
  <acceptance_criteria>
    - Validation report exists.
    - Report records `pnpm type-check` and `pnpm build` results.
    - Report includes D-01 through D-19 pass/fail rows.
    - Report includes all Phase 1 requirement IDs.
    - Report states whether each no-regression guardrail passed.
    - Report includes the route/access table with guest/admin expectations and evidence.
    - Report includes the viewport sweep table for 360px, 390px, and 430px.
    - Report includes accessibility/focus/aria evidence for bottom nav, sheets, search, Details, close, and Done controls.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human mobile and role sweep</name>
  <files>.planning/phases/01-mobile-shell-debt-payment-foundation/01-PHASE1-VALIDATION.md</files>
  <read_first>
    - .planning/phases/01-mobile-shell-debt-payment-foundation/01-UI-SPEC.md
    - .planning/phases/01-mobile-shell-debt-payment-foundation/01-PHASE1-VALIDATION.md
  </read_first>
  <action>
    Start the app with `pnpm dev` if it is not already running. As guest, manually verify `/`, `/members`, `/member/:id`, `/sessions`, `/session/:id`, `/login` direct URL, and `/create-session` protection. As admin, verify login, profile/member link, authenticated debt badge, logout to `/`, member CRUD visibility, sessions Create Session visibility, debt single QR, group QR, and admin cash confirmation where an existing admin cash entry point is available. Sweep widths 360px, 390px, and 430px. At each width verify bottom nav, group payment bar, search input, debt cards, load-more button, QR sheet footer, cash sheet footer, and toasts do not cover primary content/actions. For accessibility, use keyboard/tab checks where practical: bottom-nav tabs, search input, debt-card Details link, QR/group QR buttons, sheet close button, Done/Close button, copy button, cash review Back/Confirm buttons, and logout must be reachable with visible focus and meaningful text/aria labels. Confirm sheets expose dialog semantics (`role="dialog"`, `aria-modal="true"`), QR image alt text remains meaningful, and payment success/status is announced or visibly clear beyond color alone. Append findings to `01-PHASE1-VALIDATION.md` using the route/access and viewport sweep tables created in Task 1.
  </action>
  <verify>
    <automated>pnpm type-check && pnpm build</automated>
  </verify>
  <done>
    Done when the human sweep findings for guest/admin roles and 360px, 390px, and 430px widths are appended to `01-PHASE1-VALIDATION.md`, including bottom nav, hidden login, direct `/login`, admin controls, QR sheet order, group bar overlap, toast overlap, keyboard/focus/aria results, and payment success non-auto-close results.
  </done>
  <acceptance_criteria>
    - Human confirms bottom nav has exactly Home/Debt, Members, Sessions.
    - Human confirms guest header has no login button and language switcher remains visible.
    - Human confirms `/login` can be opened manually.
    - Human confirms guests cannot see admin CRUD/cash/create/finalize actions.
    - Human confirms QR sheet content order and non-overlap at 360/390/430px.
    - Human confirms group bar sits above bottom nav and does not cover load-more or card actions.
    - Human confirms payment success does not auto-close the sheet.
    - Human confirms toasts do not cover bottom nav, group CTA, or sheet footer actions.
    - Human confirms bottom nav, sheets, search, Details, close, Done, copy, and cash review controls have keyboard/focus paths and accessible labels/text.
  </acceptance_criteria>
  <resume-signal>Type "approved" if all sweep criteria pass, or describe each failing route/width/role.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| validation operator -> live app | Manual sweep observes app behavior across guest/admin roles. |
| browser viewport -> fixed UI controls | Fixed nav/bars/sheets/toasts can block user actions at small widths. |
| guest/admin role switch -> access controls | Incorrect UI gates can expose mutation controls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-05-01 | Elevation of privilege | Route/access sweep | mitigate | Verify guest/admin role behavior against preservation matrix. |
| T-01-05-02 | Tampering | Payment QR/cash flows | mitigate | Verify QR uses backend data and cash calls RPC only after confirmation. |
| T-01-05-03 | Denial of service | Mobile fixed controls | mitigate | Sweep 360/390/430px for blocked primary actions. |
| T-01-05-04 | Information disclosure | Hidden login assumption | mitigate | Verify no visible login affordance for guests while direct `/login` remains available. |
</threat_model>

<verification>
Overall phase verification requires `pnpm type-check`, `pnpm build`, and completed human sweep approval.
</verification>

<success_criteria>
- Automated checks pass.
- Validation report documents all locked decisions and Phase 1 requirements.
- Human sweep approves guest/admin behavior and 360/390/430px mobile ergonomics.
</success_criteria>

<output>
After completion, create `.planning/phases/01-mobile-shell-debt-payment-foundation/01-mobile-shell-debt-payment-foundation-05-SUMMARY.md`
</output>
