---
phase: 02-session-detail-task-cockpit
plan: 04
title: Costs, payments, and safe-area group payment bar
type: execute
wave: 4
depends_on:
  - 02-03
requirements:
  - SESS-07
  - SESS-08
  - SESS-09
  - SESS-10
  - SESS-12
files_modified:
  - src/views/SessionDetailView.vue
autonomous: true
must_haves:
  truths:
    - "Open sessions show live costs from calculate_session_costs with member total, interval count, court fee, shuttle fee, and surplus."
    - "Waiting-for-payment and done sessions show snapshot payments with member, must-pay amount, paid amount, status, interval count, court fee, shuttle fee, and surplus."
    - "Single QR, authenticated group QR, and admin manual cash payment entry points remain available in allowed states."
    - "The session-detail group payment bar sits above the mobile bottom nav and does not use bottom-6."
  artifacts:
    - path: "src/views/SessionDetailView.vue"
      provides: "Costs and Payments mobile cards, desktop table preservation, safe-area group bar"
      contains: "bottom-[calc(92px+env(safe-area-inset-bottom))]"
  key_links:
    - from: "Costs section"
      to: "calculate_session_costs"
      via: "fetchCosts"
      pattern: "calculate_session_costs"
    - from: "Payments section"
      to: "PaymentQRModal / ManualPaymentModal"
      via: "openPaymentQR / openCashPayment"
      pattern: "openPaymentQR|openCashPayment"
    - from: "Group payment bar"
      to: "create_group_payment"
      via: "handleCreateGroupPayment"
      pattern: "create_group_payment"
---

<objective>
Convert Costs and Payments into mobile task sections while preserving financial fields, payment contracts, and the fixed-layer safe-area rule.

Purpose: Users must see backend-owned live costs and finalized snapshots without losing payment actions or desktop financial columns.
Output: Mobile cost/payment cards, preserved desktop tables, safe-area group payment bar, and unchanged QR/cash/group payment contracts.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-session-detail-task-cockpit/02-CONTEXT.md
@.planning/phases/02-session-detail-task-cockpit/02-RESEARCH.md
@.planning/phases/02-session-detail-task-cockpit/02-UI-SPEC.md
@.planning/phases/01-mobile-shell-debt-payment-foundation/01-PHASE1-VALIDATION.md
@src/views/SessionDetailView.vue
@src/components/PaymentQRModal.vue
@src/components/ManualPaymentModal.vue
@src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mobile live-cost cards and preserve desktop cost table</name>
  <files>src/views/SessionDetailView.vue</files>
  <read_first>
    - `src/views/SessionDetailView.vue`
    - `.planning/phases/02-session-detail-task-cockpit/02-UI-SPEC.md`
  </read_first>
  <action>
    In the Costs section, keep live costs sourced from `costs` populated by `calculate_session_costs`. Add a `md:hidden` mobile card list for non-finalized sessions showing member display name, final total, interval count, court fee, shuttle fee, surplus fund summary, and `session.live` status context. If no costs exist, show `session.liveCostsEmpty`. Preserve the existing desktop cost table with all columns using `hidden md:block` or equivalent. Do not compute shared fee allocation in the frontend; only display returned `MemberCost` fields and existing presentation-only `surplus`.
  </action>
  <verify>
    <automated>grep -n "calculate_session_costs\\|liveCostsEmpty\\|costs-section\\|total_court_fee\\|total_shuttle_fee\\|final_total\\|surplus" src/views/SessionDetailView.vue</automated>
    <automated>grep -n "Math\\.round\\|court_fee_total /\\|shuttle_fee_total /" src/views/SessionDetailView.vue && exit 1 || true</automated>
  </verify>
  <done>
    Done when mobile costs show all required live cost fields from RPC output, desktop cost table remains, and no frontend fee allocation is introduced.
  </done>
  <acceptance_criteria>
    - Mobile costs show all required `MemberCost` fields.
    - Desktop cost table remains.
    - No frontend allocation logic is added.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add mobile payment snapshot cards and preserve desktop payment table</name>
  <files>src/views/SessionDetailView.vue</files>
  <read_first>
    - `src/views/SessionDetailView.vue`
    - `src/components/PaymentQRModal.vue`
    - `src/components/ManualPaymentModal.vue`
  </read_first>
  <action>
    In the Payments section for `waiting_for_payment` and `done`, add `md:hidden` snapshot cards showing member display name, must-pay/final amount, paid amount, payment status text plus color, interval count from `getBreakdown(snapshot.member_id)?.intervals_count`, court fee and shuttle fee from breakdown, surplus context, single QR action when unpaid, selection checkbox when unpaid and authenticated preserving current group QR behavior, and cash action only when unpaid and `authStore.isAdmin`. If no snapshots exist, show `session.paymentSnapshotsEmpty`. Preserve the desktop snapshot table with all existing financial/status/action columns.

    Do not rely only on template visibility for manual cash. Confirm `openCashPayment(snapshot, name)` has a handler-level `if (!authStore.isAdmin) return` guard before setting `selectedMemberId`, `selectedSnapshotMemberName`, or `showCashModal`.
  </action>
  <verify>
    <automated>grep -n "paymentSnapshotsEmpty\\|snapshot.final_amount\\|snapshot.paid_amount\\|snapshot.status\\|openPaymentQR\\|openCashPayment\\|selectedSnapshotIds" src/views/SessionDetailView.vue</automated>
    <automated>grep -n "authStore.isAdmin.*openCashPayment\\|v-if=\"snapshot.status !== 'paid' && authStore.isAdmin\"" src/views/SessionDetailView.vue</automated>
    <automated>grep -n "function openCashPayment" src/views/SessionDetailView.vue && grep -n "if (!authStore.isAdmin) return" src/views/SessionDetailView.vue</automated>
  </verify>
  <done>
    Done when mobile payment cards include every required snapshot field and action, desktop table columns remain, and manual cash stays admin-only.
  </done>
  <acceptance_criteria>
    - Mobile payment cards show final amount, paid amount, status, intervals, court fee, shuttle fee, and actions.
    - Desktop payment table remains.
    - Manual cash remains admin-only in both UI visibility and the `openCashPayment` handler.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Fix session group payment bar safe-area offset</name>
  <files>src/views/SessionDetailView.vue</files>
  <read_first>
    - `src/views/SessionDetailView.vue`
    - `src/App.vue`
    - `src/components/BottomNav.vue`
    - `.planning/phases/01-mobile-shell-debt-payment-foundation/01-VERIFICATION.md`
  </read_first>
  <action>
    Update the floating session-detail group payment bar so it never uses `bottom-6` on mobile. Use source-verifiable safe-area offset matching the Phase 2 UI spec, such as `bottom-[calc(92px+env(safe-area-inset-bottom))]`. Keep visible selected count, selected total, `handleCreateGroupPayment`, `create_group_payment`, `isCreatingGroupPayment`, and QR modal handoff through `groupPaymentData`. Ensure buttons have 44px targets and focus rings.
  </action>
  <verify>
    <automated>grep -n "bottom-6" src/views/SessionDetailView.vue && exit 1 || true</automated>
    <automated>grep -n "bottom-\\[calc(92px\\+env(safe-area-inset-bottom))\\]\\|create_group_payment\\|handleCreateGroupPayment" src/views/SessionDetailView.vue</automated>
    <automated>pnpm build</automated>
  </verify>
  <done>
    Done when the group payment bar uses a safe-area bottom offset above bottom nav, does not use `bottom-6`, preserves group QR flow, and build passes.
  </done>
  <acceptance_criteria>
    - `bottom-6` is absent from session detail.
    - Safe-area group bar offset is present.
    - `create_group_payment` flow remains.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| payment controls -> Supabase RPC | Group QR and manual cash trigger payment-related backend operations. |
| public viewer -> payment actions | Guest can create single QR where allowed; admin cash must remain protected. |
| UI financial display -> backend-owned amounts | UI displays costs/snapshots but must not allocate shared fees. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-04-01 | T | Financial display | mitigate | Display `calculate_session_costs` and `session_costs_snapshot` fields only; grep for frontend allocation patterns. |
| T-02-04-02 | E | Manual cash payment | mitigate | Render cash action only with `authStore.isAdmin`; keep `ManualPaymentModal` contract unchanged. |
| T-02-04-03 | D | Floating group payment bar | mitigate | Replace `bottom-6` with `calc(92px + env(safe-area-inset-bottom))` and keep page bottom padding from Plan 02. |
</threat_model>

<verification>
- `pnpm type-check`
- `pnpm build`
- Source checks for `calculate_session_costs`, `session_costs_snapshot`, `create_group_payment`, `add_manual_payment`, and no `bottom-6`.
</verification>

<success_criteria>
- SESS-07 through SESS-10 are fully covered.
- Costs and payments are mobile-card friendly and desktop-table complete.
- Payment modals remain reused; no schema/backend/frontend allocation changes occur.
- Known Phase 1 warning about session group bar `bottom-6` is fixed.
</success_criteria>

<output>
After completion, create `.planning/phases/02-session-detail-task-cockpit/02-session-detail-task-cockpit-04-SUMMARY.md`.
</output>
