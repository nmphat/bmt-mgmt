---
phase: 02-session-detail-task-cockpit
plan: 05
title: Realtime, polling, refresh, and no-regression validation
type: execute
wave: 5
depends_on:
  - 02-04
requirements:
  - SESS-01
  - SESS-11
  - SESS-12
files_modified:
  - src/views/SessionDetailView.vue
  - .planning/phases/02-session-detail-task-cockpit/02-PHASE2-VALIDATION.md
autonomous: true
must_haves:
  truths:
    - "Realtime subscriptions and polling remain active while session detail is mounted and are cleaned up on unmount."
    - "Manual refresh and payment-complete refresh update visible attendance/cost/payment state without stale selected payment state after close."
    - "No duplicate polling timers or realtime channels are introduced."
    - "Phase 2 validation records automated/source/build evidence and marks human UAT skipped/deferred by user instruction."
  artifacts:
    - path: "src/views/SessionDetailView.vue"
      provides: "Realtime/polling refresh preservation and cleanup"
      contains: "stopPolling"
    - path: ".planning/phases/02-session-detail-task-cockpit/02-PHASE2-VALIDATION.md"
      provides: "Phase 2 no-regression evidence"
      contains: "Human UAT skipped/deferred by user instruction"
  key_links:
    - from: "PaymentQRModal payment-complete"
      to: "fetchData"
      via: "refresh event"
      pattern: "@payment-complete=\"fetchData"
    - from: "onUnmounted"
      to: "polling/realtime/click cleanup"
      via: "stopPolling/removeChannel/removeEventListener"
      pattern: "onUnmounted"
---

<objective>
Validate realtime, polling, payment refresh, route/access, contracts, i18n, safe-area behavior, type-check, and build for Phase 2.

Purpose: Phase 2 is preservation-critical. This final plan tightens refresh behavior where needed and creates the no-regression evidence artifact without human UAT.
Output: `SessionDetailView.vue` has verified refresh lifecycle behavior; `02-PHASE2-VALIDATION.md` records automated/source validation and requirement outcomes.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-session-detail-task-cockpit/02-CONTEXT.md
@.planning/phases/02-session-detail-task-cockpit/02-RESEARCH.md
@.planning/phases/02-session-detail-task-cockpit/02-UI-SPEC.md
@.planning/phases/02-session-detail-task-cockpit/02-PRESERVATION-INVENTORY.md
@.planning/phases/01-mobile-shell-debt-payment-foundation/01-PHASE1-VALIDATION.md
@.planning/phases/01-mobile-shell-debt-payment-foundation/01-VERIFICATION.md
@src/views/SessionDetailView.vue
@src/components/PaymentQRModal.vue
@src/components/ManualPaymentModal.vue
@src/router/index.ts
@src/locales/messages.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten fetch refresh lifecycle and payment state cleanup</name>
  <files>src/views/SessionDetailView.vue</files>
  <read_first>
    - `src/views/SessionDetailView.vue`
    - `.planning/phases/02-session-detail-task-cockpit/02-RESEARCH.md`
  </read_first>
  <action>
    Review `fetchData`, realtime callbacks, polling callbacks, manual refresh, `PaymentQRModal @payment-complete`, `handleCloseQR`, and manual cash success.

    If `fetchData()` still drops refresh requests while `isFetching` is true, add a small queued-refresh flag:
    - When `fetchData` is called while `isFetching.value`, mark a pending refresh and return.
    - In `finally`, if a pending refresh exists, clear it and run one more fetch after the current fetch completes.
    - Avoid recursive loops by only allowing one queued refresh at a time.

    Ensure `startPolling()` returns if a timer exists, `stopPolling()` clears and nulls the timer, `initRealtime()` removes an existing channel before subscribing, `onUnmounted()` calls `stopPolling`, removes the document listener, and removes the realtime channel, `handleCloseQR()` clears `groupPaymentData` and `selectedSnapshotIds`, and payment completion refresh does not auto-close the QR sheet.
  </action>
  <verify>
    <automated>grep -n "isFetching\\|pendingRefresh\\|startPolling\\|stopPolling\\|removeChannel\\|onUnmounted\\|handleCloseQR\\|@payment-complete=\"fetchData" src/views/SessionDetailView.vue</automated>
    <automated>pnpm type-check</automated>
  </verify>
  <done>
    Done when refresh lifecycle avoids dropped in-flight refreshes, polling/realtime cleanup remains explicit, QR success refreshes without auto-close, and type-check passes.
  </done>
  <acceptance_criteria>
    - Pending refresh or equivalent protection exists if `fetchData` otherwise drops in-flight refreshes.
    - `startPolling`, `stopPolling`, `initRealtime`, and `onUnmounted` cleanup remain explicit.
    - QR close resets group payment state.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run Phase 2 automated/source no-regression checks</name>
  <files>src/views/SessionDetailView.vue, src/locales/messages.ts, src/router/index.ts, src/components/PaymentQRModal.vue, src/components/ManualPaymentModal.vue</files>
  <read_first>
    - `.planning/phases/02-session-detail-task-cockpit/02-PRESERVATION-INVENTORY.md`
    - `.planning/phases/02-session-detail-task-cockpit/02-UI-SPEC.md`
    - `.planning/ROADMAP.md`
  </read_first>
  <action>
    Run and capture results for the validation artifact:
    - `pnpm type-check`
    - `pnpm build`
    - Source check that `/session/:id` route has no auth/admin meta.
    - Source check that mutation controls/handlers use `isSessionEditable`/`authStore.isAdmin` and not `authStore.isAuthenticated` for session mutations.
    - Source check that all required contracts remain referenced: `view_session_summary`, `session_intervals`, `members`, `session_registrations`, `interval_presence`, `calculate_session_costs`, `session_costs_snapshot`, `finalize_session`, `sessions.update`, `add_member_to_session_full_presence`, `create_group_payment`, `add_manual_payment`.
    - Source check that `bottom-6` is absent from the session group payment bar and safe-area offset is present.
    - Source check that new Phase 2 locale keys exist in both `vi` and `en`.
    - Source check that QR/cash sheets still use `max-h-[88dvh]`, sticky footer, and dialog semantics.
  </action>
  <verify>
    <automated>pnpm type-check</automated>
    <automated>pnpm build</automated>
    <automated>grep -n "path: '/session/:id'" src/router/index.ts</automated>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/router/index.ts','utf8'); const m=s.match(/path: '\\/session\\/:id',[\\s\\S]*?component:[^\\n]+/); if(!m) throw new Error('session route not found'); if(/requiresAuth|requiresAdmin/.test(m[0])) throw new Error('/session/:id must stay public read-only')"</automated>
    <automated>grep -E "view_session_summary|session_intervals|members|session_registrations|interval_presence|calculate_session_costs|session_costs_snapshot|finalize_session|sessions|add_member_to_session_full_presence|create_group_payment|add_manual_payment" src/views/SessionDetailView.vue</automated>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/views/SessionDetailView.vue','utf8'); const updates=[...s.matchAll(/\\.from\\(['\\\"]sessions['\\\"]\\)[\\s\\S]*?\\.update\\(\\{/g)]; if(updates.length < 2) throw new Error('sessions.update edit/cancel preservation missing')"</automated>
    <automated>grep -n "authStore.isAuthenticated.*togglePresence\\|authStore.isAuthenticated.*toggleAbsent\\|authStore.isAuthenticated.*registerMembers\\|authStore.isAuthenticated.*removeRegistration\\|authStore.isAuthenticated.*openCashPayment" src/views/SessionDetailView.vue && exit 1 || true</automated>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/locales/messages.ts','utf8'); const keys=['overview','attendance','costs','payments','cockpitNavLabel','refreshSession','readOnlyHint','adminOnlyHint','lockedSessionHint','cancelledSessionHint','addMembersTitle','noRegisteredMembers','markAbsent','presentIntervals','paymentSnapshotsEmpty','liveCostsEmpty','sessionLoading','sessionLoadError','groupPaymentBar']; for (const k of keys) { const n=(s.match(new RegExp(k+':','g'))||[]).length; if(n<2) throw new Error(k+' missing locale parity') }"</automated>
    <automated>grep -n "bottom-6" src/views/SessionDetailView.vue && exit 1 || true</automated>
    <automated>grep -n "bottom-\\[calc(92px\\+env(safe-area-inset-bottom))\\]\\|pb-\\[calc(148px\\+env(safe-area-inset-bottom))\\]" src/views/SessionDetailView.vue</automated>
    <automated>grep -n "max-h-\\[88dvh\\]\\|role=\"dialog\"\\|aria-modal=\"true\"\\|sticky bottom-0" src/components/PaymentQRModal.vue src/components/ManualPaymentModal.vue</automated>
  </verify>
  <done>
    Done when type-check/build pass, all source checks pass, and no human UAT is requested.
  </done>
  <acceptance_criteria>
    - Type-check and build pass.
    - Route/access, contracts, locale parity, safe-area, modal semantics, realtime/polling checks pass.
    - No manual human UAT prompt is introduced.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create Phase 2 validation artifact</name>
  <files>.planning/phases/02-session-detail-task-cockpit/02-PHASE2-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/02-session-detail-task-cockpit/02-CONTEXT.md`
    - `.planning/phases/02-session-detail-task-cockpit/02-UI-SPEC.md`
    - `.planning/phases/02-session-detail-task-cockpit/02-PRESERVATION-INVENTORY.md`
    - `.planning/phases/01-mobile-shell-debt-payment-foundation/01-PHASE1-VALIDATION.md`
  </read_first>
  <action>
    Create `.planning/phases/02-session-detail-task-cockpit/02-PHASE2-VALIDATION.md` with automated command table for `pnpm type-check` and `pnpm build`, requirement table for SESS-01 through SESS-12 with PASS evidence, decision table for D-01 through D-21 with PASS evidence, route/access evidence for `/session/:id` public read-only route and admin-only mutations, Supabase contract preservation table, realtime/polling cleanup evidence, mobile/fixed-layer source evidence including safe-area group bar and page padding, locale parity evidence, payment sheet preservation evidence, and human UAT status `SKIPPED/DEFERRED BY USER INSTRUCTION`.
  </action>
  <verify>
    <automated>grep -E "SESS-01|SESS-12|D-01|D-21|pnpm type-check|pnpm build|Human UAT skipped/deferred by user instruction|view_session_summary|safe-area" .planning/phases/02-session-detail-task-cockpit/02-PHASE2-VALIDATION.md</automated>
  </verify>
  <done>
    Done when validation artifact exists and records automated/source evidence for all requirements and locked decisions, with human UAT explicitly skipped/deferred by user instruction.
  </done>
  <acceptance_criteria>
    - Validation artifact exists.
    - Artifact includes SESS-01 through SESS-12.
    - Artifact includes D-01 through D-21.
    - Artifact documents skipped/deferred human UAT by user instruction.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| realtime events -> visible state | Supabase change events trigger client refreshes. |
| timers/navigation -> component lifecycle | Polling timer and realtime channel must not outlive the view. |
| validation artifact -> release confidence | Automated/source validation substitutes for user-skipped human UAT. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-05-01 | D | Polling/realtime lifecycle | mitigate | Verify duplicate timer prevention and cleanup in `onUnmounted`. |
| T-02-05-02 | T | Stale visible payment state | mitigate | Ensure payment-complete refreshes data without auto-closing and explicit close resets selected group state. |
| T-02-05-03 | R | Validation evidence | mitigate | Create `02-PHASE2-VALIDATION.md` with command/source evidence and requirement/decision coverage. |
</threat_model>

<verification>
- `pnpm type-check`
- `pnpm build`
- Source checks for route access, admin locks, Supabase contracts, locale parity, fixed-layer offsets, modal semantics, realtime/polling cleanup.
</verification>

<success_criteria>
- SESS-11 is satisfied with verified realtime/polling/manual refresh/payment completion behavior.
- All Phase 2 requirements SESS-01 through SESS-12 have PASS evidence in validation.
- Human UAT is not requested and is documented as skipped/deferred by user instruction.
</success_criteria>

<output>
After completion, create `.planning/phases/02-session-detail-task-cockpit/02-session-detail-task-cockpit-05-SUMMARY.md`.
</output>
