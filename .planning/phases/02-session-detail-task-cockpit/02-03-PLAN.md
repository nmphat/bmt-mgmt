---
phase: 02-session-detail-task-cockpit
plan: 03
title: Attendance mobile cards and desktop matrix preservation
type: execute
wave: 3
depends_on:
  - 02-02
requirements:
  - SESS-01
  - SESS-04
  - SESS-05
  - SESS-06
  - SESS-12
files_modified:
  - src/views/SessionDetailView.vue
autonomous: true
must_haves:
  truths:
    - "Mobile attendance is usable as member cards while desktop keeps the interval matrix."
    - "Admins can add active members, remove registrations, toggle interval presence, and mark registered-but-absent only while the session is editable."
    - "Guests, non-admin users, waiting/done sessions, cancelled sessions, and absent members see explicit disabled/read-only states."
    - "Registered-but-absent remains visible and separate from member removal."
  artifacts:
    - path: "src/views/SessionDetailView.vue"
      provides: "Mobile attendance cards and preserved desktop matrix"
      contains: "noRegisteredMembers"
  key_links:
    - from: "attendance mobile cards"
      to: "togglePresence / toggleAbsent / removeRegistration / registerMembers"
      via: "existing handlers"
      pattern: "togglePresence|toggleAbsent|removeRegistration|registerMembers"
    - from: "desktop matrix"
      to: "interval_presence.upsert"
      via: "existing checkbox handler"
      pattern: "interval_presence.*upsert"
---

<objective>
Convert the Attendance section into a mobile task section while preserving the desktop matrix and all attendance operations.

Purpose: Attendance is the highest-risk interactive area. This plan adds a mobile card layout without changing Supabase mutation semantics or removing the existing matrix information.
Output: `SessionDetailView.vue` has mobile attendance cards, desktop matrix preservation, explicit locked states, and source-visible existing contracts.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-session-detail-task-cockpit/02-CONTEXT.md
@.planning/phases/02-session-detail-task-cockpit/02-UI-SPEC.md
@.planning/phases/02-session-detail-task-cockpit/02-PRESERVATION-INVENTORY.md
@src/views/SessionDetailView.vue
@src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework add-members control into an attendance card header</name>
  <files>src/views/SessionDetailView.vue</files>
  <read_first>
    - `src/views/SessionDetailView.vue`
    - `.planning/phases/02-session-detail-task-cockpit/02-UI-SPEC.md`
  </read_first>
  <action>
    Move the existing add-members dropdown/register button into the Attendance section as a card/header area. Show add members only when `isSessionEditable`. Keep `availableMembers`, `selectedMemberIds`, `showMemberDropdown`, `dropdownRef`, and `registerMembers` unchanged in semantics. Use `session.addMembersTitle` for the card title. Maintain 44px tap targets and focus rings on dropdown button, checkboxes, and register button. Show an explicit lock/read-only hint from `attendanceLockMessage` when not editable. Do not change the `add_member_to_session_full_presence` RPC call or selection behavior.
  </action>
  <verify>
    <automated>grep -n "addMembersTitle\\|availableMembers\\|registerMembers\\|add_member_to_session_full_presence\\|attendanceLockMessage" src/views/SessionDetailView.vue</automated>
    <automated>pnpm type-check</automated>
  </verify>
  <done>
    Done when add-member UI is in the Attendance section, uses `isSessionEditable`, keeps existing RPC semantics, and type-check passes.
  </done>
  <acceptance_criteria>
    - Add-members controls are rendered in the Attendance section.
    - `registerMembers` still calls `add_member_to_session_full_presence`.
    - Non-editable state shows a visible lock/read-only hint.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add mobile attendance member cards</name>
  <files>src/views/SessionDetailView.vue</files>
  <read_first>
    - `src/views/SessionDetailView.vue`
    - `.planning/phases/02-session-detail-task-cockpit/02-CONTEXT.md`
  </read_first>
  <action>
    Add a `md:hidden` mobile attendance card list above the desktop matrix. For each registration card, render member display name, absent chip/state, present interval count using existing `presence`, remove registration button only when `isSessionEditable`, mark absent button only when `isSessionEditable`, interval controls for every interval with formatted start/end time, disabled interval controls when not editable or when the member is registered-but-absent, and a visible disabled reason near the card when controls are locked. Use native checkbox or button controls with `min-h-11`, focus ring, and accessible labels. If `registrations.length === 0`, render `session.noRegisteredMembers`.
  </action>
  <verify>
    <automated>grep -n "md:hidden\\|noRegisteredMembers\\|presentIntervals\\|markAbsent\\|togglePresence\\|toggleAbsent\\|removeRegistration" src/views/SessionDetailView.vue</automated>
    <automated>grep -n "reg.is_registered_not_attended" src/views/SessionDetailView.vue</automated>
  </verify>
  <done>
    Done when mobile attendance cards expose every registered member and interval; absent state is visible and disables interval controls; mutation actions remain editable-only.
  </done>
  <acceptance_criteria>
    - Mobile cards render all registrations and intervals.
    - Absent state is visible and separate from removal.
    - Remove/absent/presence actions are only editable when `isSessionEditable`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Preserve and clean desktop attendance matrix</name>
  <files>src/views/SessionDetailView.vue</files>
  <read_first>
    - `src/views/SessionDetailView.vue`
    - `.planning/phases/02-session-detail-task-cockpit/02-RESEARCH.md`
  </read_first>
  <action>
    Keep the existing table-oriented attendance matrix for desktop/tablet using `hidden md:block` or equivalent. Preserve member sticky column, interval columns with start/end time, remove registration action, absent action, presence checkboxes, disabled states for non-editable and absent rows, and the `interval_presence.upsert`, `session_registrations.delete`, and `session_registrations.update({ is_registered_not_attended })` handler paths.
  </action>
  <verify>
    <automated>grep -n "hidden md:block\\|sticky left-0\\|interval_presence\\|session_registrations.*delete\\|is_registered_not_attended" src/views/SessionDetailView.vue</automated>
    <automated>pnpm build</automated>
  </verify>
  <done>
    Done when desktop matrix remains available with the same columns/actions, build passes, and no attendance contract is removed.
  </done>
  <acceptance_criteria>
    - Desktop matrix remains present.
    - Matrix keeps interval columns and member sticky column.
    - Build passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| attendance controls -> Supabase | UI controls mutate registrations and interval presence. |
| public route -> attendance cards | Guests can view attendance cards but must not mutate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-03-01 | E | Mobile attendance cards | mitigate | Render mutation controls only through `isSessionEditable`; handlers are guarded. |
| T-02-03-02 | T | Attendance state | mitigate | Keep `interval_presence.upsert` and `session_registrations.update/delete` unchanged. |
| T-02-03-03 | R | Disabled state clarity | mitigate | Show visible locked/admin-only/cancelled messaging beside disabled controls. |
</threat_model>

<verification>
- `pnpm type-check`
- `pnpm build`
- Source grep for attendance contracts and disabled states.
</verification>

<success_criteria>
- Mobile attendance cards satisfy SESS-04, SESS-05, SESS-06, and D-06 through D-09.
- Desktop matrix still exists and preserves all attendance columns/actions.
- Guests/non-admin/cancelled/waiting/done/absent states cannot mutate attendance.
</success_criteria>

<output>
After completion, create `.planning/phases/02-session-detail-task-cockpit/02-session-detail-task-cockpit-03-SUMMARY.md`.
</output>
