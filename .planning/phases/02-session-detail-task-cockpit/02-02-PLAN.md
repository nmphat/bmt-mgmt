---
phase: 02-session-detail-task-cockpit
plan: 02
title: Mobile cockpit shell, mini-tabs, and overview section
type: execute
wave: 2
depends_on:
  - 02-01
requirements:
  - SESS-02
  - SESS-03
  - SESS-12
files_modified:
  - src/views/SessionDetailView.vue
autonomous: true
must_haves:
  truths:
    - "Mobile session detail presents Overview, Attendance, Costs, and Payments as stacked task sections."
    - "Mobile users can use a sticky mini-tab row to jump to each section."
    - "Default highlighted section is status-aware: open -> Attendance, waiting/done -> Payments, cancelled -> Overview."
    - "Overview shows title, date/time, fees, status, total collected when relevant, read-only/cancelled messaging, refresh, and admin controls."
  artifacts:
    - path: "src/views/SessionDetailView.vue"
      provides: "Cockpit shell, sticky mini-tabs, overview section, safe-area page padding"
      contains: "sectionTabs"
  key_links:
    - from: "src/views/SessionDetailView.vue"
      to: "src/locales/messages.ts"
      via: "t('session.*') cockpit labels"
      pattern: "session\\.overview|session\\.cockpitNavLabel"
    - from: "sticky mini-tabs"
      to: "section cards"
      via: "stable section IDs"
      pattern: "overview-section|attendance-section|costs-section|payments-section"
---

<objective>
Build the mobile cockpit shell and Overview section while preserving existing session edit/cancel/finalize contracts.

Purpose: Establish the approved Phase 2 task-focused structure before converting the detailed attendance, costs, and payment bodies.
Output: `SessionDetailView.vue` renders a safe-area-aware mobile page with sticky mini-tabs and a compact Overview card, while desktop information remains available.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-session-detail-task-cockpit/02-CONTEXT.md
@.planning/phases/02-session-detail-task-cockpit/02-UI-SPEC.md
@.planning/phases/02-session-detail-task-cockpit/02-PRESERVATION-INVENTORY.md
@src/views/SessionDetailView.vue
@src/App.vue
@src/components/BottomNav.vue
@src/locales/messages.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add status-aware section model and anchor navigation helpers</name>
  <files>src/views/SessionDetailView.vue</files>
  <read_first>
    - `src/views/SessionDetailView.vue`
    - `.planning/phases/02-session-detail-task-cockpit/02-UI-SPEC.md`
  </read_first>
  <action>
    Add a `sectionTabs` computed array with four entries in this order: overview, attendance, costs, payments. Each entry must include stable `id`, localized `label`, and an accessible `ariaLabel`. Add `activeSection` initialized by session status after session load: `open` highlights attendance; `waiting_for_payment` and `done` highlight payments; `cancelled` highlights overview. Add `scrollToSection(sectionId)` using `document.getElementById(sectionId)?.scrollIntoView({ behavior: 'smooth', block: 'start' })`. Use native buttons in the mini-tab row and update active state on click.
  </action>
  <verify>
    <automated>grep -n "sectionTabs\|activeSection\|scrollToSection\|overview-section\|attendance-section\|costs-section\|payments-section" src/views/SessionDetailView.vue</automated>
    <automated>pnpm type-check</automated>
  </verify>
  <done>
    Done when four localized section tabs exist, default active section follows session status, and anchor navigation is source-verifiable without new dependencies.
  </done>
  <acceptance_criteria>
    - `sectionTabs`, `activeSection`, and `scrollToSection` exist.
    - Section IDs are `overview-section`, `attendance-section`, `costs-section`, and `payments-section`.
    - Type-check passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Refactor top shell and Overview into mobile-first card</name>
  <files>src/views/SessionDetailView.vue</files>
  <read_first>
    - `src/views/SessionDetailView.vue`
    - `.planning/phases/02-session-detail-task-cockpit/02-CONTEXT.md`
  </read_first>
  <action>
    Replace the top-level route content wrapper with mobile-safe classes that reserve fixed layers: use bottom padding at least `pb-[calc(148px+env(safe-area-inset-bottom))] md:pb-8` on the session detail page because the group payment bar can appear.

    Move back link and manual refresh into the Overview card/header area. The refresh button must have a visible or accessible label using `session.refreshSession`, a 44px tap target, and a focus ring.

    Build an Overview card with session title, formatted date/time, status chip with text plus semantic color, court fee, shuttle fee, total collected when `waiting_for_payment` or `done`, cancelled/read-only/admin-only/locked messaging from centralized helpers, and admin edit/cancel/finalize controls only when `isSessionEditable`. Keep edit mode functionality intact using existing `sessionForm`, `saveSession`, `cancelSession`, and `finalizeSession`.
  </action>
  <verify>
    <automated>grep -n "pb-\\[calc(148px\\+env(safe-area-inset-bottom))\\]\\|refreshSession\\|isSessionEditable\\|finalizeSession\\|cancelSession\\|saveSession" src/views/SessionDetailView.vue</automated>
    <automated>grep -n "sessions.*update\\|finalize_session" src/views/SessionDetailView.vue</automated>
  </verify>
  <done>
    Done when the Overview card contains required fields and controls, mobile page padding reserves bottom fixed layers, and edit/cancel/finalize remain tied to existing contracts and centralized locks.
  </done>
  <acceptance_criteria>
    - Overview includes title, date/time, status, fees, total collected where relevant, refresh, and admin controls.
    - `sessions.update` and `finalize_session` remain referenced.
    - Mobile bottom padding reserves the group bar and bottom nav.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Render sticky mobile mini-tabs and section shells</name>
  <files>src/views/SessionDetailView.vue</files>
  <read_first>
    - `src/views/SessionDetailView.vue`
    - `src/components/BottomNav.vue`
    - `.planning/phases/02-session-detail-task-cockpit/02-UI-SPEC.md`
  </read_first>
  <action>
    Add a sticky mini-tab row below Overview/top controls on mobile with `aria-label` using `session.cockpitNavLabel`, four native buttons with visible labels, active `aria-current` where practical, `min-h-11`, focus ring classes, and horizontal scroll if labels need space. Wrap existing attendance, costs, and payments blocks in section cards with IDs `overview-section`, `attendance-section`, `costs-section`, and `payments-section`. Detailed mobile card conversion happens in later plans.
  </action>
  <verify>
    <automated>grep -n "aria-label=.*cockpitNavLabel\\|aria-current\\|min-h-11\\|overview-section\\|attendance-section\\|costs-section\\|payments-section" src/views/SessionDetailView.vue</automated>
    <automated>pnpm build</automated>
  </verify>
  <done>
    Done when sticky mini-tabs are visible on mobile, accessible, linked to all four sections, and build passes.
  </done>
  <acceptance_criteria>
    - Sticky mini-tabs render four section buttons.
    - Each section shell has a stable ID.
    - Build passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| public viewer -> admin controls | Public users can view the route; Overview must not expose admin session mutation controls. |
| UI shell -> existing handlers | Refactoring control placement must not bypass handler guard checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-02-01 | E | Overview admin controls | mitigate | Render edit/cancel/finalize only through `isSessionEditable`; handlers remain guarded. |
| T-02-02-02 | D | Sticky navigation / fixed layers | mitigate | Use safe-area bottom padding and source-verifiable fixed-layer offsets. |
| T-02-02-03 | I | Status messaging | mitigate | Show explicit read-only/cancelled/locked text, not color-only state. |
</threat_model>

<verification>
- `pnpm type-check`
- `pnpm build`
- Source grep for section IDs, mini-tab labels, `isSessionEditable`, and safe-area bottom padding.
</verification>

<success_criteria>
- Mobile cockpit shell has four ordered sections and sticky mini-tabs.
- Overview satisfies D-01 through D-05 and SESS-02.
- Existing edit/cancel/finalize contracts remain source-visible.
</success_criteria>

<output>
After completion, create `.planning/phases/02-session-detail-task-cockpit/02-session-detail-task-cockpit-02-SUMMARY.md`.
</output>
