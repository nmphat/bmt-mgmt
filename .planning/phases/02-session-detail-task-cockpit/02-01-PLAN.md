---
phase: 02-session-detail-task-cockpit
plan: 01
title: Preservation inventory, status locks, and locale foundation
type: execute
wave: 1
depends_on: []
requirements:
  - SESS-01
  - SESS-03
  - SESS-04
  - SESS-05
  - SESS-06
files_modified:
  - src/views/SessionDetailView.vue
  - src/locales/messages.ts
  - .planning/phases/02-session-detail-task-cockpit/02-PRESERVATION-INVENTORY.md
autonomous: true
must_haves:
  truths:
    - "Guest and non-admin users can open /session/:id without seeing mutation controls."
    - "Admin mutation handlers for edit, cancel, finalize, registration, removal, attendance, and absent state only run when the session is open."
    - "Cancelled sessions are read-only for attendance, registration, removal, edit, cancel, finalize, and absent toggles."
    - "Phase 2 preservation inventory records every existing session-detail Supabase contract before layout work continues."
  artifacts:
    - path: "src/views/SessionDetailView.vue"
      provides: "Centralized session editability and mutation guards"
      contains: "isSessionEditable"
    - path: "src/locales/messages.ts"
      provides: "Vietnamese and English labels for Phase 2 cockpit locks and sections"
      contains: "cockpitNavLabel"
    - path: ".planning/phases/02-session-detail-task-cockpit/02-PRESERVATION-INVENTORY.md"
      provides: "Session-detail contract and operation inventory"
      contains: "view_session_summary"
  key_links:
    - from: "src/views/SessionDetailView.vue"
      to: "authStore.isAdmin && session.status === 'open'"
      via: "computed editability lock"
      pattern: "isSessionEditable"
    - from: "src/views/SessionDetailView.vue"
      to: "session_registrations / interval_presence / sessions.update / finalize_session"
      via: "handler guard checks"
      pattern: "if \\(!isSessionEditable\\.value\\) return"
---

<objective>
Create the preservation and lock foundation for Phase 2 before visual refactor work.

Purpose: Phase 2 changes the highest-risk screen. This plan first centralizes role/status locks, adds required localized copy, and records the existing Supabase/session operations so later layout plans can refactor without changing behavior.
Output: `SessionDetailView.vue` has centralized editability helpers and guarded handlers; `messages.ts` has Phase 2 VI/EN copy; a Phase 2 preservation inventory exists.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-session-detail-task-cockpit/02-CONTEXT.md
@.planning/phases/02-session-detail-task-cockpit/02-RESEARCH.md
@.planning/phases/02-session-detail-task-cockpit/02-UI-SPEC.md
@.planning/phases/01-mobile-shell-debt-payment-foundation/01-PHASE1-VALIDATION.md
@.planning/phases/01-mobile-shell-debt-payment-foundation/01-VERIFICATION.md
@src/views/SessionDetailView.vue
@src/locales/messages.ts
@src/types/index.ts

<interfaces>
Existing contracts in `SessionDetailView.vue` that must remain referenced:
`view_session_summary`, `session_intervals`, `members`, `session_registrations`, `interval_presence`, `calculate_session_costs`, `session_costs_snapshot`, `finalize_session`, `sessions.update`, `add_member_to_session_full_presence`, `create_group_payment`, `add_manual_payment`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralize session editability and status locks</name>
  <files>src/views/SessionDetailView.vue</files>
  <read_first>
    - `.planning/phases/02-session-detail-task-cockpit/02-CONTEXT.md`
    - `.planning/phases/02-session-detail-task-cockpit/02-RESEARCH.md`
    - `src/views/SessionDetailView.vue`
  </read_first>
  <action>
    Add computed helpers near the existing session/payment computed state:
    - `isSessionEditable = computed(() => authStore.isAdmin && session.value?.status === 'open')`
    - `isReadOnlyViewer = computed(() => !authStore.isAdmin)`
    - `isSessionFinalized = computed(() => session.value?.status === 'waiting_for_payment' || session.value?.status === 'done')`
    - `isSessionCancelled = computed(() => session.value?.status === 'cancelled')`
    - `attendanceLockMessage = computed(...)` returning localized cancelled, locked, admin-only, or read-only messaging.

    Replace repeated template checks like `authStore.isAdmin && session.status !== 'waiting_for_payment' && session.status !== 'done'` with `isSessionEditable` for edit, cancel, finalize, add members, remove registration, absent toggle, and interval presence controls.
  </action>
  <verify>
    <automated>grep -n "isSessionEditable\|isSessionFinalized\|isSessionCancelled\|attendanceLockMessage" src/views/SessionDetailView.vue</automated>
    <automated>grep -n "authStore.isAdmin && session.status !== 'waiting_for_payment'" src/views/SessionDetailView.vue && exit 1 || true</automated>
  </verify>
  <done>
    Done when centralized computed lock helpers exist, and no attendance/register/remove/absent/edit/finalize/cancel visibility condition relies on excluding waiting/done while omitting cancelled.
  </done>
  <acceptance_criteria>
    - `SessionDetailView.vue` contains `isSessionEditable`, `isReadOnlyViewer`, `isSessionFinalized`, `isSessionCancelled`, and `attendanceLockMessage`.
    - Session mutation visibility uses `isSessionEditable`.
    - Cancelled sessions cannot render editable attendance or admin session mutation controls.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Guard mutation handlers with centralized editability</name>
  <files>src/views/SessionDetailView.vue</files>
  <read_first>
    - `src/views/SessionDetailView.vue`
    - `.planning/phases/01-mobile-shell-debt-payment-foundation/01-PHASE1-VALIDATION.md`
  </read_first>
  <action>
    Update handler guards so session mutations return before touching Supabase unless allowed:
    - `saveSession`, `cancelSession`, `finalizeSession`, `registerMembers`, `removeRegistration`, `togglePresence`, and `toggleAbsent` must use `if (!isSessionEditable.value) return`.
    - Keep `openPaymentQR` public for unpaid snapshots.
    - Keep cash payment admin-only.
    - Keep `handleCreateGroupPayment` tied to selected snapshots and existing authenticated-user behavior; do not introduce frontend fee allocation.
    - Do not change RPC/table names, parameters, or route access.
  </action>
  <verify>
    <automated>grep -n "if (!isSessionEditable.value) return" src/views/SessionDetailView.vue</automated>
    <automated>pnpm type-check</automated>
  </verify>
  <done>
    Done when every session mutation handler is protected by `isSessionEditable.value`, type-check passes, and QR payment entry points remain.
  </done>
  <acceptance_criteria>
    - `saveSession`, `cancelSession`, `finalizeSession`, `registerMembers`, `removeRegistration`, `togglePresence`, and `toggleAbsent` are guarded by `isSessionEditable.value`.
    - QR payment functions are not removed.
    - No Supabase contract names or parameter shapes are changed.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add Phase 2 locale keys and preservation inventory</name>
  <files>src/locales/messages.ts, .planning/phases/02-session-detail-task-cockpit/02-PRESERVATION-INVENTORY.md</files>
  <read_first>
    - `.planning/phases/02-session-detail-task-cockpit/02-UI-SPEC.md`
    - `src/locales/messages.ts`
    - `.planning/REQUIREMENTS.md`
  </read_first>
  <action>
    Add all Phase 2 copywriting contract keys to both Vietnamese and English under `session`: `overview`, `attendance`, `costs`, `payments`, `cockpitNavLabel`, `refreshSession`, `readOnlyHint`, `adminOnlyHint`, `lockedSessionHint`, `cancelledSessionHint`, `addMembersTitle`, `noRegisteredMembers`, `markAbsent`, `presentIntervals`, `paymentSnapshotsEmpty`, `liveCostsEmpty`, `sessionLoading`, `sessionLoadError`, and `groupPaymentBar`.

    Create `.planning/phases/02-session-detail-task-cockpit/02-PRESERVATION-INVENTORY.md` with route/access expectation for `/session/:id`, operation inventory for edit/cancel/finalize/add/remove/toggle/absent/live costs/snapshots/single QR/group QR/manual cash/refresh/realtime/polling, the required Supabase contracts, and a note that human UAT is skipped/deferred by user instruction.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/locales/messages.ts','utf8'); const keys=['overview','attendance','costs','payments','cockpitNavLabel','refreshSession','readOnlyHint','adminOnlyHint','lockedSessionHint','cancelledSessionHint','addMembersTitle','noRegisteredMembers','markAbsent','presentIntervals','paymentSnapshotsEmpty','liveCostsEmpty','sessionLoading','sessionLoadError','groupPaymentBar']; for (const k of keys) { const re=new RegExp(k+':','g'); const n=(s.match(re)||[]).length; if(n<2){throw new Error(k+' missing locale parity')}}"</automated>
    <automated>grep -E "view_session_summary|calculate_session_costs|finalize_session|add_member_to_session_full_presence|create_group_payment|add_manual_payment|session_costs_snapshot|session_registrations|interval_presence|sessions.update" .planning/phases/02-session-detail-task-cockpit/02-PRESERVATION-INVENTORY.md</automated>
  </verify>
  <done>
    Done when all new keys exist in both languages, and the preservation inventory captures every required contract and operation.
  </done>
  <acceptance_criteria>
    - All new Phase 2 keys exist in both `vi` and `en`.
    - Preservation inventory includes `/session/:id`, all operations, all required contracts, and skipped/deferred human UAT note.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| client -> Supabase | UI event handlers call Supabase tables/RPCs with user-controlled route/session context. |
| public route -> mutation controls | Guests and non-admin authenticated users can view `/session/:id`; mutation affordances must remain hidden and guarded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-01-01 | E | Session mutation handlers | mitigate | Use centralized `isSessionEditable = authStore.isAdmin && session.status === 'open'` in handler guards and control visibility. |
| T-02-01-02 | T | Supabase contract usage | mitigate | Preserve existing table/RPC names and parameters; record them in `02-PRESERVATION-INVENTORY.md` for later source verification. |
| T-02-01-03 | I | Public session detail route | accept | Public read-only route is required; mitigation is preventing mutation controls/handlers for non-admins. |
</threat_model>

<verification>
- `pnpm type-check`
- Locale parity node check from Task 3.
- Source grep confirms no old incomplete status-lock pattern remains for session mutations.
</verification>

<success_criteria>
- Centralized editability helper exists and is used by all session mutation controls/handlers.
- Cancelled sessions cannot mutate attendance, registration, removal, absent state, edit, cancel, or finalize.
- Phase 2 locale keys exist in both languages.
- Preservation inventory exists and lists every required Supabase contract.
</success_criteria>

<output>
After completion, create `.planning/phases/02-session-detail-task-cockpit/02-session-detail-task-cockpit-01-SUMMARY.md`.
</output>
