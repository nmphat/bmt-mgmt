---
phase: 03-admin-supporting-screens-payment-polish-regression-pass
plan: 05
type: execute
wave: 3
depends_on: ["03-02", "03-03", "03-04"]
files_modified:
  - .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-VERIFICATION.md
autonomous: true
requirements: [ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-05, ADMIN-06]
must_haves:
  truths:
    - "Final source/build evidence confirms Phase 3 preserved route access."
    - "Final source/build evidence confirms Supabase contracts are unchanged."
    - "Final source/build evidence confirms payment, locale, fixed-layer, and table-to-card guardrails."
---

<objective>
Create the final Phase 3 and milestone no-regression validation artifact.

Purpose: ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-05, and ADMIN-06 are complete only when automated/source/build evidence confirms the refactor preserved route access, Supabase contracts, i18n parity, payment semantics, fixed layers, and table-to-card fields. Human visual UAT remains skipped/deferred per D-16.

Output: `03-VERIFICATION.md`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-CONTEXT.md
@.planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-UI-SPEC.md
@.planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-PRESERVATION-INVENTORY.md
@.planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-REGRESSION-SOURCE-CHECKS.md
@.planning/phases/01-mobile-shell-debt-payment-foundation/01-VERIFICATION.md
@.planning/phases/02-session-detail-task-cockpit/02-VERIFICATION.md
@src/router/index.ts
@src/views/DashboardView.vue
@src/views/CreateSessionView.vue
@src/views/MemberView.vue
@src/views/MemberDetailView.vue
@src/views/HomePage.vue
@src/views/SessionDetailView.vue
@src/components/PaymentQRModal.vue
@src/components/ManualPaymentModal.vue
@src/components/BottomNav.vue
@src/App.vue
@src/locales/messages.ts
</context>

<must_haves>
- D-13: final validation covers `/`, `/members`, `/member/:id`, `/sessions`, `/session/:id`, `/login`, `/create-session`.
- D-14: final validation covers all locked Supabase contracts.
- D-15: table-to-card conversions do not omit financial, status, or action fields.
- D-16: human visual UAT is skipped/deferred and replaced with automated/source/build evidence.
- D-17/D-19: locale parity and safe-area/fixed-layer evidence are recorded.
</must_haves>

<tasks>
<task type="auto">
  <name>Task 1: Run final automated type-check/build and route-access source checks</name>
  <files>.planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-VERIFICATION.md</files>
  <read_first>
    .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-REGRESSION-SOURCE-CHECKS.md,
    src/router/index.ts,
    src/views/DashboardView.vue,
    src/views/CreateSessionView.vue,
    src/views/MemberView.vue,
    src/components/PaymentQRModal.vue,
    src/components/ManualPaymentModal.vue
  </read_first>
  <action>
    Run `pnpm type-check` and `pnpm build`. Run route/access source checks:
    `grep -n "path: '/sessions'\|path: '/members'\|path: '/member/:id'\|path: '/session/:id'\|path: '/login'\|path: '/create-session'\|requiresAuth\|requiresAdmin" src/router/index.ts`

    Create or update `03-VERIFICATION.md` with frontmatter `status: in_progress` after these Task 1 checks pass. Include a section `Automated Verification` with rows for TypeScript, Production build, Route/access, Public read-only routes, Guarded create session, and Human UAT skipped/deferred. If any command fails, write `status: failed` and include the failing command and next required fix. Do not write `status: passed` in Task 1; final `status: passed` is allowed only after Task 2 completes all Supabase, payment, locale, card parity, and fixed-layer checks.
  </action>
  <acceptance_criteria>
    - `pnpm type-check` passes.
    - `pnpm build` passes.
    - `grep -n "requiresAuth: true\|requiresAdmin: true" src/router/index.ts` returns matches for `/create-session`.
    - `03-VERIFICATION.md` includes `human_uat: skipped_by_user` and route rows for `/`, `/members`, `/member/:id`, `/sessions`, `/session/:id`, `/login`, `/create-session`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm type-check && pnpm build && grep -n "requiresAuth: true\|requiresAdmin: true" src/router/index.ts && grep -n "human_uat: skipped_by_user\|/create-session\|/sessions\|/members" .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-VERIFICATION.md</automated>
  </verify>
  <done>Final verification artifact records passing type-check/build and route/access preservation evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Record Supabase, payment, locale, card parity, and fixed-layer evidence</name>
  <files>.planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-VERIFICATION.md</files>
  <read_first>
    .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-PRESERVATION-INVENTORY.md,
    .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-REGRESSION-SOURCE-CHECKS.md,
    src/views/HomePage.vue,
    src/views/DashboardView.vue,
    src/views/CreateSessionView.vue,
    src/views/MemberView.vue,
    src/views/MemberDetailView.vue,
    src/views/SessionDetailView.vue,
    src/components/PaymentQRModal.vue,
    src/components/ManualPaymentModal.vue,
    src/components/BottomNav.vue,
    src/App.vue,
    src/locales/messages.ts
  </read_first>
  <action>
    Run the Supabase, payment, locale, card parity, and fixed-layer source checks from `03-REGRESSION-SOURCE-CHECKS.md`. Append evidence sections to `03-VERIFICATION.md`:

    1. `Requirement Coverage`: ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-05, and ADMIN-06 all PASS with exact source evidence.
    2. `Supabase Contract Coverage`: record PASS for `create_session_with_intervals`, `members.insert`, `members.update`, `members.delete`, `view_member_debt_summary`, `view_member_session_details`, `view_session_summary`, `calculate_session_costs`, `finalize_session`, `add_member_to_session_full_presence`, `create_group_payment`, `add_manual_payment`, `session_costs_snapshot`, `session_registrations`, `interval_presence`, and `sessions.update`.
    3. `Payment Semantics`: record PASS for QR URL, transfer content copy, group breakdown, polling only while open, `payment-complete`, explicit close/Done, no auto-close-on-paid, two-step manual cash, and caller admin cash gate.
    4. `Locale Parity`: record the node parity command result.
    5. `Table-to-Card Parity`: record sessions list title/status/date/intervals/registrations/details, member list display name/role/active/permanent/details/admin edit/delete, and member detail debt/session history/financial/status/action fields.
    6. `Fixed-Layer Evidence`: record `max-h-[88dvh]`, `env(safe-area-inset-bottom)`, `min-h-11`, bottom nav safe-area padding, toast offset, and page bottom padding.
    7. `Warnings / Deferred Items`: include exactly that human visual UAT and 360/390/430px browser sweeps were skipped/deferred by user instruction, and existing untracked Open Design artifacts plus modified `.planning/config.json` were intentionally not committed.

    If all Task 1 and Task 2 checks pass, update the frontmatter to `status: passed`. If any Task 2 check fails, update the frontmatter to `status: failed` and include the failing command and next required fix. Do not leave `status: in_progress` after Task 2. Do not require screenshots, manual browser UAT, schema changes, RPC changes, or new test framework installation.
  </action>
  <acceptance_criteria>
    - `grep -n "^status: passed" .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-VERIFICATION.md` returns a match only after all Task 1 and Task 2 checks pass.
    - `grep -n "ADMIN-01.*PASS\|ADMIN-06.*PASS" .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-VERIFICATION.md` returns matches.
    - `grep -n "create_session_with_intervals\|members.insert\|view_member_debt_summary\|calculate_session_costs\|add_manual_payment\|sessions.update" .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-VERIFICATION.md` returns matches.
    - `grep -n "payment-complete\|explicit close\|two-step manual cash\|no auto-close-on-paid" .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-VERIFICATION.md` returns matches.
    - `grep -n "max-h-\[88dvh\]\|env(safe-area-inset-bottom)\|min-h-11\|human visual UAT" .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-VERIFICATION.md` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "ADMIN-01.*PASS\|ADMIN-06.*PASS" .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-VERIFICATION.md && grep -n "payment-complete\|two-step manual cash\|max-h-\[88dvh\]\|human visual UAT" .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-VERIFICATION.md</automated>
  </verify>
  <done>`03-VERIFICATION.md` contains final no-regression evidence across route/access, Supabase contracts, payment semantics, i18n, card parity, fixed layers, build, and type-check.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-05-01 | Repudiation | Final verification artifact | mitigate | Record exact commands and source evidence in `03-VERIFICATION.md`. |
| T-03-05-02 | Elevation of Privilege | Route/admin gates | mitigate | Verify `/create-session` meta and `authStore.isAdmin` guards source-wide. |
| T-03-05-03 | Tampering | Supabase/payment contracts | mitigate | Grep exact contract names and modal invariants before marking PASS. |
</threat_model>

<verification>
Run:
- `pnpm type-check`
- `pnpm build`
- Route/access grep
- Supabase contract grep
- Payment modal/fixed-layer grep
- Locale parity node command
- Verification artifact grep checks
</verification>

<success_criteria>
- `03-VERIFICATION.md` exists with `status: passed` only when automated/source/build checks pass.
- ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-05, and ADMIN-06 are marked PASS with source evidence.
- Human visual UAT remains documented as skipped/deferred by user instruction.
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-admin-supporting-screens-payment-polish-regression-pass-05-SUMMARY.md`
</output>
