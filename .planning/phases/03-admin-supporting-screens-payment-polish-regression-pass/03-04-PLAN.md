---
phase: 03-admin-supporting-screens-payment-polish-regression-pass
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/PaymentQRModal.vue
  - src/components/ManualPaymentModal.vue
autonomous: true
requirements: [ADMIN-05]
must_haves:
  truths:
    - "User can use QR/group QR modal as mobile sheet or desktop dialog."
    - "Payment polling runs only while open and cleanup occurs on close/unmount."
    - "Manual cash remains a two-step admin cash sheet using `add_manual_payment`."
---

<objective>
Polish the shared QR and manual payment modals across all entry points while preserving payment semantics.

Purpose: ADMIN-05 is high-risk because the shared modals are used by home, member detail, and session detail. This plan keeps payment authority in Supabase contracts and preserves explicit close/Done behavior.

Output: Updated `PaymentQRModal.vue` and `ManualPaymentModal.vue`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-CONTEXT.md
@.planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-UI-SPEC.md
@.planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-PRESERVATION-INVENTORY.md
@src/components/PaymentQRModal.vue
@src/components/ManualPaymentModal.vue
@src/views/HomePage.vue
@src/views/MemberDetailView.vue
@src/views/SessionDetailView.vue
@src/locales/messages.ts
</context>

<must_haves>
- D-09: `PaymentQRModal.vue` remains the single QR/group QR sheet for home, member detail, and session detail.
- D-10: `ManualPaymentModal.vue` remains the admin manual cash sheet.
- D-11: bottom sheet on mobile, dialog on desktop, `max-h-[88dvh]`, internal scroll, sticky safe-area footer, labelled dialog semantics, accessible close, QR alt text, 44px controls.
- D-12: no guest transfer confirmation, no auto-close-on-paid, no frontend payment allocation, no aggregate cash splitting, no new payment status authority.
</must_haves>

<tasks>
<task type="auto">
  <name>Task 1: Polish shared QR/group QR modal without changing polling semantics</name>
  <files>src/components/PaymentQRModal.vue</files>
  <read_first>
    src/components/PaymentQRModal.vue,
    src/views/HomePage.vue,
    src/views/MemberDetailView.vue,
    src/views/SessionDetailView.vue,
    src/locales/messages.ts,
    .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-UI-SPEC.md,
    .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-PRESERVATION-INVENTORY.md
  </read_first>
  <action>
    Update `PaymentQRModal.vue` per D-09, D-11, D-12. Preserve `qrUrl` construction from `BANK_INFO`, backend-owned `remainingAmount`, and `paymentInfo`. Preserve `navigator.clipboard.writeText(paymentInfo.value)`. Preserve group breakdown rendering from `props.groupData.members`. Preserve `startPolling()` guard conditions: return when `!props.show`, when `pollTimer.value` exists, and when `props.isPaid || isPaymentComplete.value`. Preserve `stopPolling()` on close, hide, paid, and `onUnmounted`. Preserve `emit('payment-complete')` and do not emit close on paid.

    Polish presentation only: keep root `role="dialog"` and `aria-modal="true"`; keep title id `modal-title`; keep panel `max-h-[88dvh]`, mobile rounded top sheet, desktop centered dialog. Ensure pending content order is amount, QR image, transfer content copy, group/member breakdown, status note/actions. Keep QR image alt meaningful. Ensure close, copy, and Done controls have `min-h-11`; footer keeps `pb-[calc(0.75rem+env(safe-area-inset-bottom))]`. Add or keep `aria-live="polite"` for success/status messaging. Do not add auto-close-on-paid or guest confirmation UI.
  </action>
  <acceptance_criteria>
    - `grep -n "BANK_INFO\|qrUrl\|navigator.clipboard.writeText\|props.groupData.members" src/components/PaymentQRModal.vue` returns matches.
    - `grep -n "startPolling\|stopPolling\|onUnmounted\|payment-complete" src/components/PaymentQRModal.vue` returns matches.
    - `grep -n "role=\"dialog\"\|aria-modal=\"true\"\|max-h-\[88dvh\]\|env(safe-area-inset-bottom)\|min-h-11\|aria-live=\"polite\"" src/components/PaymentQRModal.vue` returns matches.
    - `grep -n "@payment-complete\|@close" src/views/HomePage.vue src/views/MemberDetailView.vue src/views/SessionDetailView.vue` returns modal caller matches.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "startPolling\|stopPolling\|onUnmounted\|payment-complete" src/components/PaymentQRModal.vue && grep -n "@payment-complete\|@close" src/views/HomePage.vue src/views/MemberDetailView.vue src/views/SessionDetailView.vue && pnpm type-check</automated>
  </verify>
  <done>QR/group QR modal stays shared, responsive, accessible, explicit-close only, and timer-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Polish manual cash modal while preserving two-step RPC flow</name>
  <files>src/components/ManualPaymentModal.vue</files>
  <read_first>
    src/components/ManualPaymentModal.vue,
    src/views/SessionDetailView.vue,
    src/locales/messages.ts,
    .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-UI-SPEC.md,
    .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-PRESERVATION-INVENTORY.md
  </read_first>
  <action>
    Update `ManualPaymentModal.vue` per D-10, D-11, D-12. Preserve `currentStep: 'entry' | 'review'`, amount initialization from `snapshot.final_amount - snapshot.paid_amount`, amount > 0 validation before review and confirm, and `supabase.rpc('add_manual_payment', { p_snapshot_id, p_amount, p_note })`. Preserve `emit('success')` and `emit('close')` after RPC success, and preserve close disabled while `isSubmitting`.

    Polish presentation only: root keeps `role="dialog"` and `aria-modal="true"`; title id stays `manual-payment-title`; panel keeps `max-h-[88dvh]`, mobile bottom sheet, desktop dialog, internal scroll, sticky safe-area footer. Keep entry content for member name, remaining debt, amount, and note. Keep review content for member, amount, remaining debt, and note. Ensure close, confirm, cancel, back, and final confirm controls have `min-h-11`; focus rings remain indigo; manual cash CTA may stay green. Do not add aggregate cash allocation or caller-side admin logic inside the modal. Caller admin gate remains in `SessionDetailView.vue`.
  </action>
  <acceptance_criteria>
    - `grep -n "currentStep\|entry\|review\|amountPositiveError\|add_manual_payment\|p_snapshot_id\|p_amount\|p_note" src/components/ManualPaymentModal.vue` returns matches.
    - `grep -n "role=\"dialog\"\|aria-modal=\"true\"\|manual-payment-title\|max-h-\[88dvh\]\|env(safe-area-inset-bottom)\|min-h-11" src/components/ManualPaymentModal.vue` returns matches.
    - `grep -n "function openCashPayment\|if (!authStore.isAdmin) return\|<ManualPaymentModal" src/views/SessionDetailView.vue` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "add_manual_payment\|p_snapshot_id\|p_amount\|p_note" src/components/ManualPaymentModal.vue && grep -n "function openCashPayment\|if (!authStore.isAdmin) return\|<ManualPaymentModal" src/views/SessionDetailView.vue && pnpm type-check && pnpm build</automated>
  </verify>
  <done>Manual cash modal remains admin-caller-gated, two-step, RPC-compatible, responsive, and accessible.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-04-01 | Tampering | QR amount/content | mitigate | Preserve QR URL from backend-owned snapshot/group values; no frontend allocation. |
| T-03-04-02 | Denial of Service | Payment polling | mitigate | Preserve polling guards and cleanup on close/hide/unmount. |
| T-03-04-03 | Elevation of Privilege | Manual cash | mitigate | Preserve caller-side `authStore.isAdmin` guard in `SessionDetailView.vue`; modal does not create public cash entry. |
</threat_model>

<verification>
- `pnpm type-check`
- `pnpm build`
- Grep payment modal invariants and caller entry points.
</verification>

<success_criteria>
- ADMIN-05 passes source/build checks.
- Payment modals preserve QR URL, transfer content copy, group breakdown, polling, `payment-complete`, explicit close/Done, cleanup, and two-step manual cash.
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-admin-supporting-screens-payment-polish-regression-pass-04-SUMMARY.md`
</output>
