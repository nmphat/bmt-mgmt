---
phase: 03-admin-supporting-screens-payment-polish-regression-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-PRESERVATION-INVENTORY.md
  - .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-REGRESSION-SOURCE-CHECKS.md
  - src/locales/messages.ts
autonomous: true
requirements: [ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-05, ADMIN-06]
must_haves:
  truths:
    - "Executor has a Phase 3 route/access, Supabase-contract, payment, locale, and fixed-layer source-check inventory before UI edits."
    - "All Phase 3 labels needed by downstream plans have VI/EN parity."
  artifacts:
    - path: ".planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-PRESERVATION-INVENTORY.md"
      provides: "Phase 3 preservation inventory"
    - path: ".planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-REGRESSION-SOURCE-CHECKS.md"
      provides: "Executable source-check command list"
    - path: "src/locales/messages.ts"
      provides: "Phase 3 VI/EN copy parity"
---

<objective>
Create the Phase 3 preservation inventory and source-check scaffold before UI edits.

Purpose: Phase 3 is preservation-first. Downstream UI plans need exact route/access, Supabase, payment, i18n, and fixed-layer checks so polish work does not drift from locked decisions D-01 through D-20.

Output: `03-PRESERVATION-INVENTORY.md`, `03-REGRESSION-SOURCE-CHECKS.md`, and Phase 3 locale keys in both Vietnamese and English.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-CONTEXT.md
@.planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-RESEARCH.md
@.planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-UI-SPEC.md
@.planning/phases/01-mobile-shell-debt-payment-foundation/01-VERIFICATION.md
@.planning/phases/02-session-detail-task-cockpit/02-VERIFICATION.md
@src/router/index.ts
@src/locales/messages.ts
@src/views/DashboardView.vue
@src/views/CreateSessionView.vue
@src/views/MemberView.vue
@src/views/MemberDetailView.vue
@src/components/PaymentQRModal.vue
@src/components/ManualPaymentModal.vue
</context>

<must_haves>
- D-01 through D-20 are represented in the inventory with Full coverage.
- Deferred ideas are explicitly excluded: no schema/RPC/RLS changes, no guest transfer confirmation, no aggregate cash allocation, no offline mode, no new UI framework.
- Source checks cover `/`, `/members`, `/member/:id`, `/sessions`, `/session/:id`, `/login`, `/create-session`, all required Supabase contracts, locale parity, payment modal invariants, and fixed-layer evidence.
</must_haves>

<tasks>
<task type="auto">
  <name>Task 1: Create Phase 3 preservation inventory and regression source checks</name>
  <files>
    .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-PRESERVATION-INVENTORY.md,
    .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-REGRESSION-SOURCE-CHECKS.md
  </files>
  <read_first>
    .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-CONTEXT.md,
    .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-RESEARCH.md,
    .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-UI-SPEC.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md,
    src/router/index.ts,
    src/views/DashboardView.vue,
    src/views/CreateSessionView.vue,
    src/views/MemberView.vue,
    src/views/MemberDetailView.vue,
    src/components/PaymentQRModal.vue,
    src/components/ManualPaymentModal.vue
  </read_first>
  <action>
    Create `03-PRESERVATION-INVENTORY.md` with sections for Route Access, Supabase Contracts, UI Field Parity, Payment Semantics, Locale Parity, Fixed Layers, Deferred/Forbidden Items, and Decision Coverage Matrix. Mark every D-01 through D-20 as Full. Include ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-05, and ADMIN-06 mapping. Explicitly document: `/sessions` public read-only per D-01; `/create-session` requires `requiresAuth` and `requiresAdmin` per D-03; `/members` public read-only per D-05; QR/manual payment semantics per D-09 through D-12; final validation requirements per D-13 through D-16; copy/visual/safe-area rules per D-17 through D-20.

    Create `03-REGRESSION-SOURCE-CHECKS.md` with copy-pasteable commands only. Include these exact check groups: route/access grep; admin guard grep; Supabase contract grep; payment modal/fixed-layer grep; locale parity `node -e` check; `pnpm type-check`; `pnpm build`. Do not require human visual UAT; document it as skipped/deferred per D-16.
  </action>
  <acceptance_criteria>
    - `grep -n "D-01.*Full\|D-20.*Full" .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-PRESERVATION-INVENTORY.md` returns matches.
    - `grep -n "create_session_with_intervals\|members.insert\|members.update\|members.delete\|view_member_debt_summary\|view_member_session_details\|view_session_summary\|calculate_session_costs\|finalize_session\|add_member_to_session_full_presence\|create_group_payment\|add_manual_payment\|session_costs_snapshot\|session_registrations\|interval_presence\|sessions.update" .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-PRESERVATION-INVENTORY.md` returns matches.
    - `grep -n "pnpm type-check\|pnpm build\|role=\"dialog\"\|max-h-\[88dvh\]" .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-REGRESSION-SOURCE-CHECKS.md` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "D-01.*Full\|D-20.*Full" .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-PRESERVATION-INVENTORY.md && grep -n "pnpm type-check\|pnpm build" .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-REGRESSION-SOURCE-CHECKS.md</automated>
  </verify>
  <done>Phase 3 preservation inventory and executable source-check command list exist and fully cover ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-05, ADMIN-06 plus D-01 through D-20.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 3 locale keys with VI/EN parity</name>
  <files>src/locales/messages.ts</files>
  <read_first>
    src/locales/messages.ts,
    .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-UI-SPEC.md,
    .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-CONTEXT.md
  </read_first>
  <action>
    Update `src/locales/messages.ts` per D-17 so downstream plans can use localized labels without adding one-sided copy. Add or update these exact keys in both `vi` and `en` blocks:

    `dashboard.loadError`: VI `Không tải được danh sách buổi. Hãy thử lại hoặc báo quản trị viên nếu lỗi tiếp tục.` EN `Could not load sessions. Try again or contact an admin if it continues.`
    `dashboard.sessionCardAria`: VI `Xem chi tiết buổi {title}` EN `View session details for {title}`
    `createSession.createError`: VI `Không tạo được buổi. Kiểm tra thông tin và thử lại.` EN `Could not create the session. Check the details and try again.`
    `member.emptyState`: VI `Chưa có thành viên nào.` EN `No members yet.`
    Update existing `member.fetchError` to the UI-SPEC copy: VI `Không tải được danh sách thành viên. Hãy thử lại hoặc báo quản trị viên nếu lỗi tiếp tục.` EN `Could not load members. Try again or contact an admin if it continues.`
    `member.activeStatus`: VI `Đang hoạt động` EN `Active`
    `member.inactiveStatus`: VI `Ngừng hoạt động` EN `Inactive`
    `member.permanentStatus`: VI `Cố định` EN `Permanent`
    `member.temporaryStatus`: VI `Không cố định` EN `Temporary`
    `member.viewDetailsFor`: VI `Xem chi tiết {name}` EN `View details for {name}`
    `member.editMember`: VI `Sửa {name}` EN `Edit {name}`
    `member.deleteMember`: VI `Xóa {name}` EN `Delete {name}`

    Do not add a guest-facing login label. Do not add copy for deferred guest transfer confirmation, aggregate cash allocation, offline mode, or new payment methods.
  </action>
  <acceptance_criteria>
    - `grep -n "loadError\|sessionCardAria\|createError\|emptyState\|activeStatus\|inactiveStatus\|permanentStatus\|temporaryStatus\|viewDetailsFor\|editMember\|deleteMember" src/locales/messages.ts` returns matches in both locale blocks.
    - `node -e "const fs=require('fs'); const s=fs.readFileSync('src/locales/messages.ts','utf8'); for (const k of ['loadError','sessionCardAria','createError','emptyState','activeStatus','inactiveStatus','permanentStatus','temporaryStatus','viewDetailsFor','editMember','deleteMember']) { const n=(s.match(new RegExp(k,'g'))||[]).length; if(n<2){throw new Error(k+' missing VI/EN parity')}} console.log('locale parity ok')"` passes.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('src/locales/messages.ts','utf8'); for (const k of ['loadError','sessionCardAria','createError','emptyState','activeStatus','inactiveStatus','permanentStatus','temporaryStatus','viewDetailsFor','editMember','deleteMember']) { const n=(s.match(new RegExp(k,'g'))||[]).length; if(n<2){throw new Error(k+' missing VI/EN parity')}} console.log('locale parity ok')" && pnpm type-check</automated>
  </verify>
  <done>Phase 3 labels needed by sessions, create-session, and members plans exist in both Vietnamese and English with no deferred feature copy.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-01-01 | Elevation of Privilege | Route/access inventory | mitigate | Source-check `/create-session` still has `requiresAuth` and `requiresAdmin`; require handler/visibility guard checks in downstream plans. |
| T-03-01-02 | Tampering | Supabase contract list | mitigate | Inventory lists exact RPC/view/table contracts; downstream plans must grep for unchanged names. |
</threat_model>

<verification>
Run:
- `pnpm type-check`
- Locale parity `node -e` command from Task 2
- Grep commands in `03-REGRESSION-SOURCE-CHECKS.md`
</verification>

<success_criteria>
- Preservation inventory exists and maps D-01 through D-20 to Full coverage.
- Regression source checks exist and include route/access, Supabase contract, payment invariant, fixed-layer, locale parity, type-check, and build commands.
- Phase 3 locale keys have VI/EN parity.
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-admin-supporting-screens-payment-polish-regression-pass-01-SUMMARY.md`
</output>
