---
phase: 03-admin-supporting-screens-payment-polish-regression-pass
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/views/DashboardView.vue
  - src/views/CreateSessionView.vue
autonomous: true
requirements: [ADMIN-01, ADMIN-02, ADMIN-06]
must_haves:
  truths:
    - "User can view `/sessions` read-only and open session detail."
    - "Only admins see the Create Session affordance."
    - "Admin can create a session through the existing `create_session_with_intervals` payload."
---

<objective>
Polish the sessions list and create-session form while preserving public read-only access and admin-only session creation.

Purpose: ADMIN-01 and ADMIN-02 require the remaining admin/supporting session screens to use the approved mobile-first design language without changing route access or Supabase contracts.

Output: Updated `DashboardView.vue` and `CreateSessionView.vue`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-CONTEXT.md
@.planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-UI-SPEC.md
@.planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-PRESERVATION-INVENTORY.md
@src/router/index.ts
@src/views/DashboardView.vue
@src/views/CreateSessionView.vue
@src/locales/messages.ts
</context>

<must_haves>
- D-01: `/sessions` remains public read-only.
- D-02: cards preserve title, status, date, interval count, registration count, and detail navigation.
- D-03: `/create-session` remains route-guarded by `requiresAuth` and `requiresAdmin`; do not edit router guard.
- D-04: form polish improves density and safe spacing without recurrence/court/default-member/fee-splitting additions.
- D-17/D-18/D-19: use existing locale keys, local Tailwind primitives, 44px targets, and safe bottom spacing.
</must_haves>

<tasks>
<task type="auto">
  <name>Task 1: Polish sessions list mobile cards and error state</name>
  <files>src/views/DashboardView.vue</files>
  <read_first>
    src/views/DashboardView.vue,
    src/router/index.ts,
    src/locales/messages.ts,
    .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-UI-SPEC.md,
    .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-PRESERVATION-INVENTORY.md
  </read_first>
  <action>
    Update `DashboardView.vue` per D-01 and D-02. Preserve `supabase.from('view_session_summary').select('*').order('session_date', { ascending: false })`. Add `errorMessage = ref('')` and `useToast()`; clear `errorMessage` before fetch; on catch set `errorMessage.value = t.value('dashboard.loadError')` and call `toast.error(t.value('dashboard.loadError'))`. Keep `v-if="authStore.isAdmin"` on the Create Session router-link.

    Adjust the template to use a mobile-first shell: outer wrapper `max-w-7xl mx-auto px-4 py-6 sm:px-6 lg:px-8`; header `flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between`; admin Create Session button `inline-flex min-h-11 items-center justify-center rounded-xl bg-indigo-600 px-4 py-2 text-sm font-bold text-white`. Cards must be white, rounded-xl, border-gray-200, shadow-sm, focus-visible indigo ring, and contain: title, status chip, formatted date, interval count, registration count, and a visible `t('dashboard.viewDetails')` detail row with `ChevronRight`. Add `:aria-label="t('dashboard.sessionCardAria', { title: session.title })"` on the session card router-link. Keep desktop/tablet responsive grid.
  </action>
  <acceptance_criteria>
    - `grep -n "view_session_summary\|authStore.isAdmin\|dashboard.loadError\|dashboard.sessionCardAria" src/views/DashboardView.vue` returns matches.
    - `grep -n "total_intervals\|total_registrations\|dashboard.viewDetails\|/session/" src/views/DashboardView.vue` returns matches.
    - `grep -n "min-h-11\|focus-visible.*indigo\|rounded-xl" src/views/DashboardView.vue` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "view_session_summary\|authStore.isAdmin\|dashboard.loadError\|dashboard.sessionCardAria" src/views/DashboardView.vue && pnpm type-check</automated>
  </verify>
  <done>`/sessions` keeps public read-only cards with all required fields and admin-only create affordance.</done>
</task>

<task type="auto">
  <name>Task 2: Polish guarded create-session mobile form without RPC drift</name>
  <files>src/views/CreateSessionView.vue</files>
  <read_first>
    src/views/CreateSessionView.vue,
    src/router/index.ts,
    src/locales/messages.ts,
    .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-UI-SPEC.md,
    .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-PRESERVATION-INVENTORY.md
  </read_first>
  <action>
    Update `CreateSessionView.vue` per D-03 and D-04. Do not change `src/router/index.ts`; verify route meta remains `requiresAuth: true, requiresAdmin: true`. Preserve the form fields `title`, `date`, `startTime`, `endTime`, `courtFee`, and `shuttleFee`. Preserve end-time validation with `startDateTime.value >= endDateTime.value` and `toast.error(t.value('createSession.endTimeError'))`. Preserve the RPC name and exact payload keys: `p_title`, `p_start_time`, `p_end_time`, `p_court_fee`, `p_shuttle_fee`, `p_created_by`. Change only the fallback error toast to `toast.error(error.message || t.value('createSession.createError'))`.

    Polish layout with outer wrapper `max-w-2xl mx-auto px-4 py-6 sm:px-6 lg:px-8`, card `rounded-2xl border border-gray-200 bg-white p-4 shadow-sm sm:p-6`, labels `text-sm font-bold text-gray-700`, every input/select class including `min-h-11`, and submit button `min-h-11 w-full rounded-xl bg-indigo-600 px-4 py-3 text-base font-bold text-white`. Keep date/time fields readable at 360px: use `grid grid-cols-1 gap-4 sm:grid-cols-2` for time and fee groups. Do not add recurrence, court selection, default members, fee allocation, schema changes, or new dependencies.
  </action>
  <acceptance_criteria>
    - `grep -n "create_session_with_intervals\|p_title\|p_start_time\|p_end_time\|p_court_fee\|p_shuttle_fee\|p_created_by" src/views/CreateSessionView.vue` returns matches.
    - `grep -n "createSession.endTimeError\|createSession.createError\|loading" src/views/CreateSessionView.vue` returns matches.
    - `grep -n "min-h-11\|grid-cols-1 gap-4 sm:grid-cols-2\|rounded-2xl" src/views/CreateSessionView.vue` returns matches.
    - `grep -n "path: '/create-session'\|requiresAuth: true\|requiresAdmin: true" src/router/index.ts` still returns matches.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "create_session_with_intervals\|p_title\|p_start_time\|p_end_time\|p_court_fee\|p_shuttle_fee\|p_created_by" src/views/CreateSessionView.vue && grep -n "requiresAuth: true\|requiresAdmin: true" src/router/index.ts && pnpm type-check && pnpm build</automated>
  </verify>
  <done>Create-session remains admin-guarded and RPC-compatible, with mobile-safe fields and submit affordance.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-02-01 | Elevation of Privilege | Dashboard create CTA | mitigate | Keep `v-if="authStore.isAdmin"` and router guard unchanged. |
| T-03-02-02 | Tampering | Create-session RPC payload | mitigate | Preserve exact RPC name and payload keys; no frontend interval generation. |
</threat_model>

<verification>
- `pnpm type-check`
- `pnpm build`
- Source grep for `view_session_summary`, admin CTA guard, create-session route meta, and `create_session_with_intervals` payload keys.
</verification>

<success_criteria>
- ADMIN-01 and ADMIN-02 pass source/build checks.
- ADMIN-06 session-card field preservation is source-verifiable.
- No route/access or Supabase contract drift.
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-admin-supporting-screens-payment-polish-regression-pass-02-SUMMARY.md`
</output>
