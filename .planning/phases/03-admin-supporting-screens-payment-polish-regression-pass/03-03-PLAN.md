---
phase: 03-admin-supporting-screens-payment-polish-regression-pass
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/views/MemberView.vue
autonomous: true
requirements: [ADMIN-03, ADMIN-06]
must_haves:
  truths:
    - "User can view member list on mobile as cards with display name, role, active state, permanent state, and Details navigation."
    - "Admins can add, edit, and delete members with existing semantics preserved."
    - "Desktop table information remains available."
---

<objective>
Add member-list mobile cards while preserving desktop table and member CRUD behavior.

Purpose: ADMIN-03 and ADMIN-06 require a mobile-first member list that does not drop role/status/action data or weaken admin gates.

Output: Updated `MemberView.vue`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-CONTEXT.md
@.planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-UI-SPEC.md
@.planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-PRESERVATION-INVENTORY.md
@src/views/MemberView.vue
@src/router/index.ts
@src/locales/messages.ts
</context>

<must_haves>
- D-05: `/members` remains public read-only for viewing and member detail navigation.
- D-06: cards preserve display name, role, active, permanent, Details, and admin edit/delete.
- D-07: add/edit/delete semantics preserve `members.insert`, `members.update`, `members.delete`, role, active/permanent, create-another, loading, toast, and confirmation.
- D-15: table-to-card conversion does not omit status or action fields.
- D-17/D-18/D-19: VI/EN labels, local Tailwind primitives, 44px tap targets, safe spacing.
</must_haves>

<tasks>
<task type="auto">
  <name>Task 1: Add mobile member cards while preserving desktop table</name>
  <files>src/views/MemberView.vue</files>
  <read_first>
    src/views/MemberView.vue,
    src/router/index.ts,
    src/locales/messages.ts,
    .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-UI-SPEC.md,
    .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-PRESERVATION-INVENTORY.md
  </read_first>
  <action>
    Update `MemberView.vue` per D-05, D-06, D-15. Keep the existing Supabase fetch and locale sort. Keep the existing desktop table but wrap it in `hidden overflow-x-auto md:block`. Add an additive `md:hidden` card list before the table. For each mobile card, render display name, role chip (`admin` indigo, `member` gray), active state chip using `member.activeStatus` or `member.inactiveStatus`, permanent state chip using `member.permanentStatus` or `member.temporaryStatus`, a `router-link` to `/member/${member.id}` with visible `t('debt.details')`, and admin-only edit/delete buttons with `v-if="authStore.isAdmin"`.

    Use white cards with `rounded-2xl border border-gray-200 bg-white p-4 shadow-sm`, card actions with `min-h-11`, and visible focus rings. Add an empty state when `members.length === 0` using `t('member.emptyState')`. Do not remove the desktop table columns for name, role, active, permanent, and actions.
  </action>
  <acceptance_criteria>
    - `grep -n "md:hidden\|hidden overflow-x-auto md:block\|member.emptyState" src/views/MemberView.vue` returns matches.
    - `grep -n "member.activeStatus\|member.inactiveStatus\|member.permanentStatus\|member.temporaryStatus" src/views/MemberView.vue` returns matches.
    - `grep -n "/member/\|debt.details\|authStore.isAdmin" src/views/MemberView.vue` returns matches.
    - Desktop table still contains `member.name`, `member.role`, `member.active`, `member.permanent`, and `common.actions`.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "md:hidden\|hidden overflow-x-auto md:block\|member.emptyState" src/views/MemberView.vue && grep -n "member.name\|member.role\|member.active\|member.permanent\|common.actions" src/views/MemberView.vue && pnpm type-check</automated>
  </verify>
  <done>Mobile cards and desktop table both preserve member identity, status, route action, and admin actions.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve CRUD gates and make member forms mobile-safe</name>
  <files>src/views/MemberView.vue</files>
  <read_first>
    src/views/MemberView.vue,
    src/locales/messages.ts,
    .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-UI-SPEC.md,
    .planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-PRESERVATION-INVENTORY.md
  </read_first>
  <action>
    Update add/edit controls per D-07 and D-19 without changing Supabase calls. Preserve handler guards exactly: `if (!authStore.isAdmin) return` in `addMember`, `deleteMember`, `startEdit`, and `saveEdit`. Preserve `supabase.from('members').insert([newMember.value]).select()`, `supabase.from('members').update(updates).eq('id', id)`, and `supabase.from('members').delete().eq('id', id)`. Preserve `createAnother`, `actionLoading`, toast success/error calls, delete confirmation with `member.deleteConfirm`, and fields `display_name`, `role`, `is_active`, `is_permanent`.

    Polish the add form: card `rounded-2xl`, responsive `grid grid-cols-1 gap-4 md:grid-cols-4`, every input/select `min-h-11`, checkbox labels `min-h-11 items-center`, submit/cancel `min-h-11`. For mobile editing, when `editingMemberId === member.id` in the card list, render editable display name, role select, active checkbox row, permanent checkbox row, Save and Cancel buttons with visible labels and `min-h-11`. Admin edit/delete buttons must have `:aria-label="t('member.editMember', { name: member.display_name })"` and `:aria-label="t('member.deleteMember', { name: member.display_name })"`.
  </action>
  <acceptance_criteria>
    - `grep -n "if (!authStore.isAdmin) return" src/views/MemberView.vue` returns at least four matches.
    - `grep -n "from('members').insert\|from('members').update\|from('members').delete" src/views/MemberView.vue` returns matches.
    - `grep -n "createAnother\|actionLoading\|member.deleteConfirm\|toast.success" src/views/MemberView.vue` returns matches.
    - `grep -n "member.editMember\|member.deleteMember\|min-h-11" src/views/MemberView.vue` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "from('members').insert\|from('members').update\|from('members').delete" src/views/MemberView.vue && grep -n "if (!authStore.isAdmin) return" src/views/MemberView.vue && pnpm type-check && pnpm build</automated>
  </verify>
  <done>Member add/edit/delete behavior, fields, loading, create-another, toasts, confirmation, and admin gates are preserved with mobile-safe controls.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-03-01 | Elevation of Privilege | Member CRUD | mitigate | Preserve both `v-if="authStore.isAdmin"` and handler-level `if (!authStore.isAdmin) return`. |
| T-03-03-02 | Tampering | Member update payload | mitigate | Preserve existing fields only: display name, role, active, permanent, updated_at. |
</threat_model>

<verification>
- `pnpm type-check`
- `pnpm build`
- Source grep for `members.insert`, `members.update`, `members.delete`, admin guards, `md:hidden`, preserved desktop table.
</verification>

<success_criteria>
- ADMIN-03 passes source/build checks.
- ADMIN-06 passes for member-list table-to-card parity.
- `/members` remains public read-only with admin-only mutation controls.
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-supporting-screens-payment-polish-regression-pass/03-admin-supporting-screens-payment-polish-regression-pass-03-SUMMARY.md`
</output>
